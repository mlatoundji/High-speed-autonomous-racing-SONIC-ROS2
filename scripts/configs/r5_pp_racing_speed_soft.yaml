# R5 — Pure Pursuit + racing line: cruise / lateral g-limit / soft-error weights
#
# Based on r3_pp_racing_optional.yaml. Explores speed–grip trade-offs and how
# velocity planner soft penalties interact with PP on the racing line.
#
#   python3 scripts/benchmark.py --config scripts/configs/r5_pp_racing_speed_soft.yaml
#
# Wall-clock: order-of-magnitude ~30–40 min × N runs (2 laps + 1 warmup per launch)
#
# Parameter legend: values with trailing ``# TUNED …`` differ from ``r5_sp_baseline`` in this file.
# All other keys match that baseline.

- stack: pure_pursuit
  profile: r5_sp_baseline
  line: racing
  latency_ms: 0
  odom_noise_std: 0.0
  lap_count: 2
  warmup_laps: 1
  camera: false  # drop third-person camera sensor (CPU saving); set true to see it in Gazebo
  navigation:
    # Baseline: full r3_pp_racing_optional reference; no TUNED keys in this profile.
    localisation:
      ros__parameters:
        update_frequency: 50.0
    local_planner:
      ros__parameters:
        update_frequency: 10.0
        car_width: 2.0
        centreofgravity_to_frontaxle: 1.483
        frame_id: "base_link"
        cruise_velocity: 8.0
        avoid_velocity: 8.0
        max_lateral_accel: 5.5
        min_curvature: 0.012
        curvature_lookahead: 140
        curvature_smooth_window: 5
        accel_rate: 5.0
        decel_rate: 7.0
    global_planner:
      ros__parameters:
        update_frequency: 2.0
        waypoints_ahead: 5
        waypoints_behind: 2
        passed_threshold: 0.25
        centreofgravity_to_frontaxle: 1.483
        waypoint_search_ahead: 30
        waypoints_file: waypoints.csv
    path_tracker:
      ros__parameters:
        update_frequency: 50.0
        centreofgravity_to_frontaxle: 1.483
        centreofgravity_to_rearaxle: 1.483
        wheelbase: 2.966
        lookahead_gain: 0.4
        lookahead_min: 1.5
        lookahead_max: 6.0
        closest_search_ahead: 120
        steering_limits: 0.95
        steering_rate_limit: 4.0  # capped (was 0.0=unlimited): avoids instant steering snaps -> Gazebo physics shock -> NaN/reset
        steer_smoothing: 1.0
        velocity_gain: 1.0
        startup_ramp_s: 2.0
        lateral_soft: 4.0
        heading_soft: 0.6

- stack: pure_pursuit
  profile: r5_sp_slower_high_g
  line: racing
  latency_ms: 0
  odom_noise_std: 0.0
  lap_count: 2
  warmup_laps: 1
  camera: false  # drop third-person camera sensor (CPU saving); set true to see it in Gazebo
  navigation:
    # Deltas vs r5_sp_baseline: local_planner cruise/avoid velocity, max_lateral_accel only.
    # --- frozen (same as baseline): localisation, global_planner, path_tracker ---
    localisation:
      ros__parameters:
        update_frequency: 50.0
    local_planner:
      ros__parameters:
        update_frequency: 10.0
        car_width: 2.0
        centreofgravity_to_frontaxle: 1.483
        frame_id: "base_link"
        cruise_velocity: 7.5  # TUNED (baseline 8.0)
        avoid_velocity: 7.5  # TUNED (baseline 8.0)
        max_lateral_accel: 6.0  # TUNED (baseline 5.5)
        min_curvature: 0.012
        curvature_lookahead: 140
        curvature_smooth_window: 5
        accel_rate: 5.0
        decel_rate: 7.0
    global_planner:
      ros__parameters:
        update_frequency: 2.0
        waypoints_ahead: 5
        waypoints_behind: 2
        passed_threshold: 0.25
        centreofgravity_to_frontaxle: 1.483
        waypoint_search_ahead: 30
        waypoints_file: waypoints.csv
    path_tracker:
      ros__parameters:
        update_frequency: 50.0
        centreofgravity_to_frontaxle: 1.483
        centreofgravity_to_rearaxle: 1.483
        wheelbase: 2.966
        lookahead_gain: 0.4
        lookahead_min: 1.5
        lookahead_max: 6.0
        closest_search_ahead: 120
        steering_limits: 0.95
        steering_rate_limit: 4.0  # capped (was 0.0=unlimited): avoids instant steering snaps -> Gazebo physics shock -> NaN/reset
        steer_smoothing: 1.0
        velocity_gain: 1.0
        startup_ramp_s: 2.0
        lateral_soft: 4.0
        heading_soft: 0.6

- stack: pure_pursuit
  profile: r5_sp_faster_cap_g
  line: racing
  latency_ms: 0
  odom_noise_std: 0.0
  lap_count: 2
  warmup_laps: 1
  camera: false  # drop third-person camera sensor (CPU saving); set true to see it in Gazebo
  navigation:
    # Deltas vs r5_sp_baseline: local_planner cruise/avoid velocity, max_lateral_accel only.
    # --- frozen (same as baseline): localisation, global_planner, path_tracker ---
    localisation:
      ros__parameters:
        update_frequency: 50.0
    local_planner:
      ros__parameters:
        update_frequency: 10.0
        car_width: 2.0
        centreofgravity_to_frontaxle: 1.483
        frame_id: "base_link"
        cruise_velocity: 8.5  # TUNED (baseline 8.0)
        avoid_velocity: 8.5  # TUNED (baseline 8.0)
        max_lateral_accel: 5.0  # TUNED (baseline 5.5)
        min_curvature: 0.012
        curvature_lookahead: 140
        curvature_smooth_window: 5
        accel_rate: 5.0
        decel_rate: 7.0
    global_planner:
      ros__parameters:
        update_frequency: 2.0
        waypoints_ahead: 5
        waypoints_behind: 2
        passed_threshold: 0.25
        centreofgravity_to_frontaxle: 1.483
        waypoint_search_ahead: 30
        waypoints_file: waypoints.csv
    path_tracker:
      ros__parameters:
        update_frequency: 50.0
        centreofgravity_to_frontaxle: 1.483
        centreofgravity_to_rearaxle: 1.483
        wheelbase: 2.966
        lookahead_gain: 0.4
        lookahead_min: 1.5
        lookahead_max: 6.0
        closest_search_ahead: 120
        steering_limits: 0.95
        steering_rate_limit: 4.0  # capped (was 0.0=unlimited): avoids instant steering snaps -> Gazebo physics shock -> NaN/reset
        steer_smoothing: 1.0
        velocity_gain: 1.0
        startup_ramp_s: 2.0
        lateral_soft: 4.0
        heading_soft: 0.6

- stack: pure_pursuit
  profile: r5_sp_soft_strict_path
  line: racing
  latency_ms: 0
  odom_noise_std: 0.0
  lap_count: 2
  warmup_laps: 1
  camera: false  # drop third-person camera sensor (CPU saving); set true to see it in Gazebo
  navigation:
    # Deltas vs r5_sp_baseline: path_tracker lateral_soft, heading_soft only.
    # --- frozen (same as baseline): localisation, local_planner, global_planner ---
    localisation:
      ros__parameters:
        update_frequency: 50.0
    local_planner:
      ros__parameters:
        update_frequency: 10.0
        car_width: 2.0
        centreofgravity_to_frontaxle: 1.483
        frame_id: "base_link"
        cruise_velocity: 8.0
        avoid_velocity: 8.0
        max_lateral_accel: 5.5
        min_curvature: 0.012
        curvature_lookahead: 140
        curvature_smooth_window: 5
        accel_rate: 5.0
        decel_rate: 7.0
    global_planner:
      ros__parameters:
        update_frequency: 2.0
        waypoints_ahead: 5
        waypoints_behind: 2
        passed_threshold: 0.25
        centreofgravity_to_frontaxle: 1.483
        waypoint_search_ahead: 30
        waypoints_file: waypoints.csv
    path_tracker:
      ros__parameters:
        update_frequency: 50.0
        centreofgravity_to_frontaxle: 1.483
        centreofgravity_to_rearaxle: 1.483
        wheelbase: 2.966
        lookahead_gain: 0.4
        lookahead_min: 1.5
        lookahead_max: 6.0
        closest_search_ahead: 120
        steering_limits: 0.95
        steering_rate_limit: 4.0  # capped (was 0.0=unlimited): avoids instant steering snaps -> Gazebo physics shock -> NaN/reset
        steer_smoothing: 1.0
        velocity_gain: 1.0
        startup_ramp_s: 2.0
        lateral_soft: 5.5  # TUNED (baseline 4.0); stronger cross-track penalty
        heading_soft: 0.45  # TUNED (baseline 0.6)

- stack: pure_pursuit
  profile: r5_sp_soft_loose_time
  line: racing
  latency_ms: 0
  odom_noise_std: 0.0
  lap_count: 2
  warmup_laps: 1
  camera: false  # drop third-person camera sensor (CPU saving); set true to see it in Gazebo
  navigation:
    # Deltas vs r5_sp_baseline: local_planner accel_rate, decel_rate; path_tracker lateral_soft, heading_soft.
    # --- frozen: every key below unless it has a # TUNED comment ---
    localisation:
      ros__parameters:
        update_frequency: 50.0
    local_planner:
      ros__parameters:
        update_frequency: 10.0
        car_width: 2.0
        centreofgravity_to_frontaxle: 1.483
        frame_id: "base_link"
        cruise_velocity: 8.0
        avoid_velocity: 8.0
        max_lateral_accel: 5.5
        min_curvature: 0.012
        curvature_lookahead: 140
        curvature_smooth_window: 5
        accel_rate: 5.5  # TUNED (baseline 5.0)
        decel_rate: 7.5  # TUNED (baseline 7.0)
    global_planner:
      ros__parameters:
        update_frequency: 2.0
        waypoints_ahead: 5
        waypoints_behind: 2
        passed_threshold: 0.25
        centreofgravity_to_frontaxle: 1.483
        waypoint_search_ahead: 30
        waypoints_file: waypoints.csv
    path_tracker:
      ros__parameters:
        update_frequency: 50.0
        centreofgravity_to_frontaxle: 1.483
        centreofgravity_to_rearaxle: 1.483
        wheelbase: 2.966
        lookahead_gain: 0.4
        lookahead_min: 1.5
        lookahead_max: 6.0
        closest_search_ahead: 120
        steering_limits: 0.95
        steering_rate_limit: 4.0  # capped (was 0.0=unlimited): avoids instant steering snaps -> Gazebo physics shock -> NaN/reset
        steer_smoothing: 1.0
        velocity_gain: 1.0
        startup_ramp_s: 2.0
        lateral_soft: 3.0  # TUNED (baseline 4.0)
        heading_soft: 0.85  # TUNED (baseline 0.6)
